# Showcase assets

Presentation assets for the repository landing page.

## Current required asset — PRESENT

| File | Purpose | Status |
|---|---|---|
| `kang-ai-autocut-hero.png` | Hero banner at the top of every README entry point | **PRESENT** |

```
dimensions   1949 x 807
size         1,719,348 bytes
sha256       64231fc3f252b6470dae476cb4ce7f653d0dc571cfede7ea3fd4c30b6935b506
```

Produced by the Producer's design flow and committed **exactly as provided** — not
regenerated, not cropped, not recompressed, not re-encoded.

All seven README entry points reference this **one** file by repository-relative path; the
image is not duplicated per language.

## The workflow is not an asset — NOT REQUIRED

README-native **Mermaid** is the current workflow visualisation, present in all seven
READMEs. It needs no image, no external dependency, and stays maintainable as the workflow
changes.

A workflow **PNG is NOT REQUIRED**. A long-form workflow infographic is a social-media and
communication asset, not a GitHub README dependency, and no such file belongs in this
directory.

## Future optional assets

These remain **optional** future showcase assets, not current requirements:

| File | Purpose |
|---|---|
| `variant-preview.gif` | short looping preview of a produced variant |
| `variant-comparison.png` | two variants side by side |
| `contact-sheet.png` | shot contact sheet from a produced variant |
| `before-after.png` | raw footage beside the produced result |

Each is **production media** and is added only alongside a production record in this
repository, so anything shown here can be traced to a real run. No illustrative or
fabricated media belongs in this directory, and no production metrics are invented to
populate it.
